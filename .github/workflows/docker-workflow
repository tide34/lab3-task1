name: Docker Image CI

on:
  push:
    branches: 
	- main

jobs:

  action:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build -t rubydocker ../ 
